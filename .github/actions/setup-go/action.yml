name: Setup Go
description: Set up Go (version from go.mod, in lockstep) with module/build caching. Run actions/checkout BEFORE this.
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v7
      with:
        go-version-file: go.mod
        cache: true
